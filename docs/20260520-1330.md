# 핸드오프 — 내일 바로 이어서 (2025-05-20 13:30)

> **이 문서는 "내일 즉시 재개"용 액션 핸드오프.** 전체 변경 이력·Why는 [`20260520-1100.md`](20260520-1100.md) (M1~M10) 참조.

## 0. 한 줄 요약
정시(어디가 백분위)·수시(내신 등급) 진단 + 솔루션 리포트까지 완성됨. **현재 작업: 34개 PDF 전수 데이터 품질검증·정제.** 추출까지 돌려놓고 마무리 → 내일 정제·적재만 하면 됨.

---

## 1. 지금 어디까지 됐나 (완료)
- ✅ 정시/수시 양 모드, 4단계 신호(안정/적정/소신/위험), 솔루션 리포트(근거·과목진단·민감도·전략), 출처 라벨, 고교유형 수시환경 안내, 입결없음 정직 처리.
- ✅ 표 기반 추출기 `extract_adiga_2025.py` (병합셀 forward-fill, 정시 Format A/B, 수시 등급, 노이즈 필터).
- ✅ 품질 정제기 `build_app_data.py` 작성 완료 (이상치·노이즈 제거 + 앱 적재 CSV 생성 + 리포트).
- ✅ Java `StagingDataLoader.loadSusi`가 정제본 `kcue_susi_cutoffs.csv`를 읽도록 연결됨.
- ✅ **34개 PDF 전부 `data-staging/` 루트에 모음** (전국 17개 시도. `기타` 폴더 비움).

## 2. 34개 추출 — ✅ 완료됨 (어젯밤 13:5x)
- `extract_adiga_2025.py *.pdf` 완료. 결과: **정시 1,559행/62개 대학, 수시 2,395행/51개 대학** (전국 17개 시도).
- 산출물 `integrated_{jeongsi,susi}_results_2025.csv` 갱신됨 → **내일 Step 2(추출) 스킵 가능.** CSV 수정시각이 어제 13시 이후면 재추출 불필요.
- 아직 **앱 적재 전**(이전 22개분 49/40대학이 DB에 있음) → 내일 Step 3·4로 62/51대학 정제·교체.

---

## 3. 내일 할 일 (정확한 순서) ⭐

### Step 1. 서버 띄우기 (재부팅했다면)
```powershell
# 터미널 1 — 백엔드
cd C:\Users\i7E-87\작업중\university\backend
.\gradlew.bat bootRun        # "Started DiagnosticApplication" 뜨면 OK, 켜둠
# 터미널 2 — 프론트
cd C:\Users\i7E-87\작업중\university\frontend
npm run dev                  # http://localhost:5173
```
※ NEIS_API_KEY는 backend/.env 에 이미 있음 → 부팅 시 고교 2,408개 자동 적재.

### Step 2. 34개 PDF 추출 (어젯밤 완료됐으면 스킵)
```powershell
cd C:\Users\i7E-87\작업중\university\backend\data-staging
# 완료 확인: integrated_*.csv 수정시각이 어젯밤 이후면 스킵
python extract_adiga_2025.py *.pdf     # ~22분. JEONGSI/SUSI 행수 출력됨
```

### Step 3. 품질 전수검증·정제 (← 다음 레벨 핵심)
```powershell
python build_app_data.py               # kcue_domestic_cutoffs.csv + kcue_susi_cutoffs.csv 생성
notepad _quality_report.txt            # 제외된 이상치/노이즈 검토 (연세대 외 오추출 색출)
```
- 리포트에서 잘못 걸러진/남은 게 있으면 `build_app_data.py`의 임계(정시 ±20pp, 수시 ±2.5등급) 조정.

### Step 4. 적재 + 검증
```powershell
curl -X POST http://localhost:8080/api/ingest/staging/reload   # 재시작 없이 반영
```
- PowerShell에서 검증(한글 깨짐 방지 UTF-8):
```powershell
[Console]::OutputEncoding=[System.Text.Encoding]::UTF8
# 정시: 연세대 경영 = 93.8 근처, 국어국문 = 93.0 근처 (Format B 정상값)
# 수시: 건국대 경영 내신2.5 → 안정
```

---

## 4. 미완 / 다음 후보 (시간 날 때)
1. **데이터 품질 마무리** — Step3 리포트 보고 임계 튜닝. 전 대학(49+) 스팟체크.
2. **누락 검토** — 34개로 전국 커버되나, 일부 대학이 정시 백분위 미공개(환산점수/등급만)라 빠질 수 있음(출처 한계, 정상).
3. (보류) 표준점수 도입 / 다년치 추세 / 모집요강 반영비율 — README 신뢰선 내 "정밀도" 항목. **모집요강은 사용자가 패스 결정함.**

## 5. 신뢰 범위 (절대 원칙 — README "목적과 신뢰 범위" 참조)
- ✅ 정량 위치 0단계 스크리닝 / 정보 비대칭 해소.
- ❌ 합불 확률 정밀예측·표준점수·정성평가·실시간동향 = 학원 컨설팅 영역, 넘지 말 것.
- 데이터는 어디가 공식값, 반영비율은 계열 추정치(결과에 출처 명시됨).

## 6. 주요 파일 위치
| 용도 | 경로 |
|---|---|
| 추출기 | `backend/data-staging/extract_adiga_2025.py` |
| 정제기 | `backend/data-staging/build_app_data.py` |
| 원본 산출물 | `data-staging/integrated_{jeongsi,susi}_results_2025.csv` (utf-8-sig) |
| 앱 적재본 | `data-staging/kcue_domestic_cutoffs.csv`, `kcue_susi_cutoffs.csv` |
| 원천 PDF | `data-staging/*.pdf` (34개) |
| 진단 로직 | `backend/.../service/{Domestic,Global,Susi}DiagnosticService.java`, `Signal.java`, `SolutionService.java` |
| 결과 UI | `frontend/src/components/DiagnosticResult.jsx`, `DiagnosticInputForm.jsx`, `App.jsx` |

## 7. 임시파일 정리 메모
탐색용 `_*.txt` 일부 남아있을 수 있음(예: `_quality_report.txt`는 보존). 불필요 시 `_seoul_dump.txt` 등 `_` 시작 파일 정리 가능(단 `_quality_report.txt`, `_convert_xlsx_to_csv.py` 제외).
